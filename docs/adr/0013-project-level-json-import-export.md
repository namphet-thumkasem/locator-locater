# Project-Level JSON Import Export

The first version supports JSON import and export at the Project boundary rather than full catalog backup or server sync. This gives users a practical way to back up, move, or share locator work while keeping catalog data browser-owned.
