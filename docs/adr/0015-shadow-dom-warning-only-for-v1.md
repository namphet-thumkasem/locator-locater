# Shadow DOM Warning Only For V1

Shadow DOM is not a core v1 locator target. The app may detect likely shadow boundaries and show warnings, but full shadow DOM serialization, validation, and framework-specific locator output are deferred until the basic page snapshot workflow is proven.
