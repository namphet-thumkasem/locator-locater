# MVP1 Excludes URL Ingestion Implementation

MVP-1 focuses on pasted HTML and rendered HTML bundles so the locator workbench can prove selection, candidate generation, validation, and snippet copying before server-side fetching is added. The UI may reserve space for a future URL input, but URL ingestion is not implemented in the first milestone.
