# Browser-Owned Catalog Data For V1

Catalog data for the first version is owned by the user's browser storage, with JSON import and export for backup or device transfer. The Next.js server endpoint is limited to URL ingestion so the product can avoid accounts, tenancy, and server-side catalog persistence until sharing becomes a proven need.
