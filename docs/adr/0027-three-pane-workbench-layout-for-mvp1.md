# Three Pane Workbench Layout For MVP1

MVP-1 uses a three-pane workbench layout on desktop: input and capture tools on the left, snapshot preview in the center, and action target details plus locator candidates on the right. This keeps selection and decision-making visible at the same time instead of forcing a step-by-step wizard.
