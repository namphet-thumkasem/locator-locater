# Snapshot Preview Not Live Iframe Capture

The preview iframe renders sanitized Page Snapshots for selection, not live third-party pages for capture. Authenticated and client-rendered pages are handled in v1 by Rendered HTML Paste, because cross-origin iframe content usually cannot be inspected and many applications block embedding entirely.
