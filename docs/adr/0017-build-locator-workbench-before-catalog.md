# Build Locator Workbench Before Catalog

The first implementation milestone is the Locator Workbench: import HTML, create a Page Snapshot, select an action target, generate and validate locator candidates, and copy snippets. Catalog persistence, revalidation, project import/export, URL ingestion polish, and bookmarklet polish are deferred until the workbench proves that locator generation is useful.
