# Next.js Frontend Only For MVP1

MVP-1 uses Next.js even though the first workbench can run entirely in the browser. No backend route is required until URL ingestion is implemented, allowing the project to keep the intended deployment path without building server code before it has a product use.
