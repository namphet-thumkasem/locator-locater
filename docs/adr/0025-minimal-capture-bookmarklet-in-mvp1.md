# Minimal Capture Bookmarklet In MVP1

MVP-1 includes a minimal capture bookmarklet that produces a Rendered HTML Bundle and copies it to the clipboard, falling back to a prompt when clipboard access fails. It does not auto-send data to the app, compress payloads, inline computed styles, or capture a selected element.
