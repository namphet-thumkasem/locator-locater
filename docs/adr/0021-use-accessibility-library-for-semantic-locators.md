# Use Accessibility Library For Semantic Locators

MVP-1 uses an accessibility-focused DOM helper library to derive roles and accessible names for semantic locator candidates instead of hand-rolling the rules. These candidates are still validated against the Page Snapshot and are not presented as a perfect substitute for a live browser accessibility tree.
