# Playwright Primary With Tiered Framework Formatters

Playwright is the primary formatter for v1 because its semantic locator model aligns best with locator candidate ranking. Cypress, Selenium, and generic output are still supported, but formatters may show compatibility warnings when a candidate cannot be represented faithfully or idiomatically in that framework.
