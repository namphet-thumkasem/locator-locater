# Static Page Snapshots For V1

For the first version, imported pages are converted into sanitized static Page Snapshots without running the page's JavaScript. This keeps the core locator generation and catalog workflow small, safe, and testable while accepting that JavaScript-rendered pages will need a later browser-capture workflow.
